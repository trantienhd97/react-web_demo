import * as React from 'react';
import { Text, View } from 'react-native';

export const App = React.FC = () => {
  return (
    <View style={{backgroundColor: 'red', height: '100%'}}>
      <Text>Welcome to TranTien</Text>
    </View>
  );
}

export default App;
